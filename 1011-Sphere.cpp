#include <stdio.h>

int main() {

    float R; //R = Radius
    double ; //V = Volume
    const double pi = 3.14159;

    scanf("%f",&R);

    V = ((4.0 * pi * R*R*R) / 3);
    printf("VOLUME = %.3lf\n", V);

    return 0;
}
